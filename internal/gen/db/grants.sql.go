// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: grants.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createGrant = `-- name: CreateGrant :one
INSERT INTO grants (
    id, region, user_id, client_id, pairwise_sub, scopes
) VALUES (
    $1, $2, $3, $4, $5, $6
)
RETURNING id, region, user_id, client_id, pairwise_sub, scopes, created_at, revoked_at
`

type CreateGrantParams struct {
	ID          pgtype.UUID `json:"id"`
	Region      string      `json:"region"`
	UserID      pgtype.UUID `json:"user_id"`
	ClientID    string      `json:"client_id"`
	PairwiseSub string      `json:"pairwise_sub"`
	Scopes      []string    `json:"scopes"`
}

func (q *Queries) CreateGrant(ctx context.Context, arg CreateGrantParams) (Grant, error) {
	row := q.db.QueryRow(ctx, createGrant,
		arg.ID,
		arg.Region,
		arg.UserID,
		arg.ClientID,
		arg.PairwiseSub,
		arg.Scopes,
	)
	var i Grant
	err := row.Scan(
		&i.ID,
		&i.Region,
		&i.UserID,
		&i.ClientID,
		&i.PairwiseSub,
		&i.Scopes,
		&i.CreatedAt,
		&i.RevokedAt,
	)
	return i, err
}

const findGrantByPPID = `-- name: FindGrantByPPID :one
SELECT id, region, user_id, client_id, pairwise_sub, scopes, created_at, revoked_at FROM grants
WHERE pairwise_sub = $1
  AND client_id = $2
  AND revoked_at IS NULL
`

type FindGrantByPPIDParams struct {
	PairwiseSub string `json:"pairwise_sub"`
	ClientID    string `json:"client_id"`
}

// FindGrantByPPID looks up an active grant by its pairwise_sub (PPID) and
// client_id. Used during RP-Initiated Logout to reverse-lookup the userID from
// the id_token_hint's sub claim without exposing internal user IDs to RPs.
func (q *Queries) FindGrantByPPID(ctx context.Context, arg FindGrantByPPIDParams) (Grant, error) {
	row := q.db.QueryRow(ctx, findGrantByPPID, arg.PairwiseSub, arg.ClientID)
	var i Grant
	err := row.Scan(
		&i.ID,
		&i.Region,
		&i.UserID,
		&i.ClientID,
		&i.PairwiseSub,
		&i.Scopes,
		&i.CreatedAt,
		&i.RevokedAt,
	)
	return i, err
}

const findGrantByUserClient = `-- name: FindGrantByUserClient :one
SELECT id, region, user_id, client_id, pairwise_sub, scopes, created_at, revoked_at FROM grants
WHERE user_id = $1
  AND client_id = $2
  AND revoked_at IS NULL
`

type FindGrantByUserClientParams struct {
	UserID   pgtype.UUID `json:"user_id"`
	ClientID string      `json:"client_id"`
}

func (q *Queries) FindGrantByUserClient(ctx context.Context, arg FindGrantByUserClientParams) (Grant, error) {
	row := q.db.QueryRow(ctx, findGrantByUserClient, arg.UserID, arg.ClientID)
	var i Grant
	err := row.Scan(
		&i.ID,
		&i.Region,
		&i.UserID,
		&i.ClientID,
		&i.PairwiseSub,
		&i.Scopes,
		&i.CreatedAt,
		&i.RevokedAt,
	)
	return i, err
}

const getGrant = `-- name: GetGrant :one

SELECT id, region, user_id, client_id, pairwise_sub, scopes, created_at, revoked_at FROM grants
WHERE id = $1
`

// Queries for the grants table. The query IS the contract (DESIGN §1.3):
// `sqlc generate` (via @codegen) produces typed Go — never hand-write DB types.
func (q *Queries) GetGrant(ctx context.Context, id pgtype.UUID) (Grant, error) {
	row := q.db.QueryRow(ctx, getGrant, id)
	var i Grant
	err := row.Scan(
		&i.ID,
		&i.Region,
		&i.UserID,
		&i.ClientID,
		&i.PairwiseSub,
		&i.Scopes,
		&i.CreatedAt,
		&i.RevokedAt,
	)
	return i, err
}

const listGrantsByClient = `-- name: ListGrantsByClient :many
SELECT id, region, user_id, client_id, pairwise_sub, scopes, created_at, revoked_at FROM grants
WHERE client_id = $1
  AND revoked_at IS NULL
ORDER BY created_at DESC
`

// ListGrantsByClient returns all active grants for a specific client. Used
// during client deletion (RFC 7592) to identify affected users.
func (q *Queries) ListGrantsByClient(ctx context.Context, clientID string) ([]Grant, error) {
	rows, err := q.db.Query(ctx, listGrantsByClient, clientID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Grant
	for rows.Next() {
		var i Grant
		if err := rows.Scan(
			&i.ID,
			&i.Region,
			&i.UserID,
			&i.ClientID,
			&i.PairwiseSub,
			&i.Scopes,
			&i.CreatedAt,
			&i.RevokedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listGrantsByUser = `-- name: ListGrantsByUser :many
SELECT id, region, user_id, client_id, pairwise_sub, scopes, created_at, revoked_at FROM grants
WHERE user_id = $1
  AND revoked_at IS NULL
ORDER BY created_at DESC
`

func (q *Queries) ListGrantsByUser(ctx context.Context, userID pgtype.UUID) ([]Grant, error) {
	rows, err := q.db.Query(ctx, listGrantsByUser, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Grant
	for rows.Next() {
		var i Grant
		if err := rows.Scan(
			&i.ID,
			&i.Region,
			&i.UserID,
			&i.ClientID,
			&i.PairwiseSub,
			&i.Scopes,
			&i.CreatedAt,
			&i.RevokedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const revokeGrant = `-- name: RevokeGrant :exec
UPDATE grants
SET revoked_at = now()
WHERE id = $1
  AND revoked_at IS NULL
`

func (q *Queries) RevokeGrant(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, revokeGrant, id)
	return err
}

const revokeGrantsByClient = `-- name: RevokeGrantsByClient :exec
UPDATE grants
SET revoked_at = now()
WHERE client_id = $1
  AND revoked_at IS NULL
`

// RevokeGrantsByClient revokes all active grants for a specific client. Used
// during client deletion (RFC 7592) to clean up user authorizations.
func (q *Queries) RevokeGrantsByClient(ctx context.Context, clientID string) error {
	_, err := q.db.Exec(ctx, revokeGrantsByClient, clientID)
	return err
}
