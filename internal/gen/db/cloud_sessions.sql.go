// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: cloud_sessions.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createCloudSession = `-- name: CreateCloudSession :one

INSERT INTO cloud_sessions (
    session_id, namespace_id, token_hash, expires_at
) VALUES (
    $1, $2, $3, $4
)
RETURNING session_id, namespace_id, token_hash, expires_at, consumed_at, created_at
`

type CreateCloudSessionParams struct {
	SessionID   string             `json:"session_id"`
	NamespaceID string             `json:"namespace_id"`
	TokenHash   []byte             `json:"token_hash"`
	ExpiresAt   pgtype.Timestamptz `json:"expires_at"`
}

// Queries for the cloud_sessions table — short-lived, namespace-scoped
// provisioning credentials minted for Harbor Cloud (Harbor Cloud management
// API contract; openspec/changes/harbor-cloud-management-api-contract-2ee993ea).
// Not an OIDC/BFF session. The query IS the contract (DESIGN §1.3): `sqlc
// generate` (via @codegen) produces typed Go — never hand-write DB types.
// Mints a namespace-scoped session. Only token_hash is persisted; the
// plaintext bearer is returned once by the caller at mint time.
func (q *Queries) CreateCloudSession(ctx context.Context, arg CreateCloudSessionParams) (CloudSession, error) {
	row := q.db.QueryRow(ctx, createCloudSession,
		arg.SessionID,
		arg.NamespaceID,
		arg.TokenHash,
		arg.ExpiresAt,
	)
	var i CloudSession
	err := row.Scan(
		&i.SessionID,
		&i.NamespaceID,
		&i.TokenHash,
		&i.ExpiresAt,
		&i.ConsumedAt,
		&i.CreatedAt,
	)
	return i, err
}

const getCloudSession = `-- name: GetCloudSession :one
SELECT session_id, namespace_id, token_hash, expires_at, consumed_at, created_at FROM cloud_sessions
WHERE session_id = $1
`

// Returns the session row regardless of expiry/consumption — the caller
// decides 410 session_expired / 403 cross_tenant_forbidden from the
// returned fields rather than have the store hide lifecycle state.
func (q *Queries) GetCloudSession(ctx context.Context, sessionID string) (CloudSession, error) {
	row := q.db.QueryRow(ctx, getCloudSession, sessionID)
	var i CloudSession
	err := row.Scan(
		&i.SessionID,
		&i.NamespaceID,
		&i.TokenHash,
		&i.ExpiresAt,
		&i.ConsumedAt,
		&i.CreatedAt,
	)
	return i, err
}
