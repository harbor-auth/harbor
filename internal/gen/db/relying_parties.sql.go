// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: relying_parties.sql

package db

import (
	"context"
)

const getRelyingParty = `-- name: GetRelyingParty :one

SELECT client_id, name, sector_id, redirect_uris, token_format, scopes_allowed FROM relying_parties
WHERE client_id = $1
`

// Queries for the relying_parties table (RP/client registry; DESIGN §10, §3.2).
// The query IS the contract (DESIGN §1.3): `sqlc generate` (via @codegen)
// produces typed Go — never hand-write DB types.
func (q *Queries) GetRelyingParty(ctx context.Context, clientID string) (RelyingParty, error) {
	row := q.db.QueryRow(ctx, getRelyingParty, clientID)
	var i RelyingParty
	err := row.Scan(
		&i.ClientID,
		&i.Name,
		&i.SectorID,
		&i.RedirectUris,
		&i.TokenFormat,
		&i.ScopesAllowed,
	)
	return i, err
}

const listRelyingParties = `-- name: ListRelyingParties :many
SELECT client_id, name, sector_id, redirect_uris, token_format, scopes_allowed FROM relying_parties
ORDER BY client_id
`

func (q *Queries) ListRelyingParties(ctx context.Context) ([]RelyingParty, error) {
	rows, err := q.db.Query(ctx, listRelyingParties)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []RelyingParty
	for rows.Next() {
		var i RelyingParty
		if err := rows.Scan(
			&i.ClientID,
			&i.Name,
			&i.SectorID,
			&i.RedirectUris,
			&i.TokenFormat,
			&i.ScopesAllowed,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertRelyingParty = `-- name: UpsertRelyingParty :one
INSERT INTO relying_parties (
    client_id, name, sector_id, redirect_uris, token_format, scopes_allowed
) VALUES (
    $1, $2, $3, $4, $5, $6
)
ON CONFLICT (client_id) DO UPDATE
    SET name           = EXCLUDED.name,
        sector_id      = EXCLUDED.sector_id,
        redirect_uris  = EXCLUDED.redirect_uris,
        token_format   = EXCLUDED.token_format,
        scopes_allowed = EXCLUDED.scopes_allowed
RETURNING client_id, name, sector_id, redirect_uris, token_format, scopes_allowed
`

type UpsertRelyingPartyParams struct {
	ClientID      string   `json:"client_id"`
	Name          string   `json:"name"`
	SectorID      string   `json:"sector_id"`
	RedirectUris  []string `json:"redirect_uris"`
	TokenFormat   string   `json:"token_format"`
	ScopesAllowed []string `json:"scopes_allowed"`
}

func (q *Queries) UpsertRelyingParty(ctx context.Context, arg UpsertRelyingPartyParams) (RelyingParty, error) {
	row := q.db.QueryRow(ctx, upsertRelyingParty,
		arg.ClientID,
		arg.Name,
		arg.SectorID,
		arg.RedirectUris,
		arg.TokenFormat,
		arg.ScopesAllowed,
	)
	var i RelyingParty
	err := row.Scan(
		&i.ClientID,
		&i.Name,
		&i.SectorID,
		&i.RedirectUris,
		&i.TokenFormat,
		&i.ScopesAllowed,
	)
	return i, err
}
