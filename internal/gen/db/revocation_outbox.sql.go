// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: revocation_outbox.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const countPendingRevocations = `-- name: CountPendingRevocations :one
SELECT COUNT(*) FROM revocation_outbox
WHERE status = 'pending'
`

// Count pending revocations (for monitoring/alerting).
func (q *Queries) CountPendingRevocations(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countPendingRevocations)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const deleteDeliveredRevocations = `-- name: DeleteDeliveredRevocations :exec
DELETE FROM revocation_outbox
WHERE status = 'delivered'
  AND created_at < $1
`

// Clean up delivered revocation signals older than the retention period.
// Background cleanup, off the hot path.
func (q *Queries) DeleteDeliveredRevocations(ctx context.Context, createdAt pgtype.Timestamptz) error {
	_, err := q.db.Exec(ctx, deleteDeliveredRevocations, createdAt)
	return err
}

const enqueueRevocation = `-- name: EnqueueRevocation :one

INSERT INTO revocation_outbox (
    reason, user_id, client_id, grant_id
) VALUES (
    $1, $2, $3, $4
)
RETURNING id, reason, user_id, client_id, grant_id, status, retry_count, next_attempt_at, created_at
`

type EnqueueRevocationParams struct {
	Reason   string      `json:"reason"`
	UserID   pgtype.UUID `json:"user_id"`
	ClientID string      `json:"client_id"`
	GrantID  pgtype.UUID `json:"grant_id"`
}

// Queries for the revocation_outbox table (durable theft-signal delivery;
// DESIGN §3.5, §3.5.2, §10). The query IS the contract (DESIGN §1.3):
// `sqlc generate` (via @codegen) produces typed Go — never hand-write DB types.
//
// The outbox pattern: signalRefreshReuse/signalCodeReuse INSERT here first,
// then a background worker polls and delivers with retry.
// Enqueue a revocation signal for durable delivery. Called by signalRefreshReuse
// and signalCodeReuse after (or instead of) the inline best-effort attempt.
func (q *Queries) EnqueueRevocation(ctx context.Context, arg EnqueueRevocationParams) (RevocationOutbox, error) {
	row := q.db.QueryRow(ctx, enqueueRevocation,
		arg.Reason,
		arg.UserID,
		arg.ClientID,
		arg.GrantID,
	)
	var i RevocationOutbox
	err := row.Scan(
		&i.ID,
		&i.Reason,
		&i.UserID,
		&i.ClientID,
		&i.GrantID,
		&i.Status,
		&i.RetryCount,
		&i.NextAttemptAt,
		&i.CreatedAt,
	)
	return i, err
}

const fetchPendingRevocations = `-- name: FetchPendingRevocations :many
SELECT id, reason, user_id, client_id, grant_id, status, retry_count, next_attempt_at, created_at FROM revocation_outbox
WHERE status = 'pending'
  AND next_attempt_at <= now()
ORDER BY next_attempt_at ASC
LIMIT $1
FOR UPDATE SKIP LOCKED
`

// Fetch pending revocation signals ready for delivery. Uses FOR UPDATE SKIP
// LOCKED to allow multiple workers without contention. Limited to batch_size
// rows to avoid long transactions. Only fetches rows whose next_attempt_at
// has passed (respecting exponential backoff).
func (q *Queries) FetchPendingRevocations(ctx context.Context, limit int32) ([]RevocationOutbox, error) {
	rows, err := q.db.Query(ctx, fetchPendingRevocations, limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []RevocationOutbox
	for rows.Next() {
		var i RevocationOutbox
		if err := rows.Scan(
			&i.ID,
			&i.Reason,
			&i.UserID,
			&i.ClientID,
			&i.GrantID,
			&i.Status,
			&i.RetryCount,
			&i.NextAttemptAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getRevocation = `-- name: GetRevocation :one
SELECT id, reason, user_id, client_id, grant_id, status, retry_count, next_attempt_at, created_at FROM revocation_outbox
WHERE id = $1
`

// Get a single revocation entry by ID (for debugging/admin).
func (q *Queries) GetRevocation(ctx context.Context, id pgtype.UUID) (RevocationOutbox, error) {
	row := q.db.QueryRow(ctx, getRevocation, id)
	var i RevocationOutbox
	err := row.Scan(
		&i.ID,
		&i.Reason,
		&i.UserID,
		&i.ClientID,
		&i.GrantID,
		&i.Status,
		&i.RetryCount,
		&i.NextAttemptAt,
		&i.CreatedAt,
	)
	return i, err
}

const incrementRevocationRetry = `-- name: IncrementRevocationRetry :exec
UPDATE revocation_outbox
SET retry_count = retry_count + 1,
    next_attempt_at = $2
WHERE id = $1
`

type IncrementRevocationRetryParams struct {
	ID            pgtype.UUID        `json:"id"`
	NextAttemptAt pgtype.Timestamptz `json:"next_attempt_at"`
}

// Increment retry count and set next attempt time with exponential backoff.
// Called when delivery fails but retries remain. The caller computes
// next_attempt_at based on the retry policy (5s, 30s, 5m, 30m, 1h cap).
func (q *Queries) IncrementRevocationRetry(ctx context.Context, arg IncrementRevocationRetryParams) error {
	_, err := q.db.Exec(ctx, incrementRevocationRetry, arg.ID, arg.NextAttemptAt)
	return err
}

const markRevocationDelivered = `-- name: MarkRevocationDelivered :exec
UPDATE revocation_outbox
SET status = 'delivered'
WHERE id = $1
`

// Mark a revocation signal as successfully delivered. Called after
// RevocationSink.RevokeSessionsByUserClient succeeds.
func (q *Queries) MarkRevocationDelivered(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, markRevocationDelivered, id)
	return err
}

const markRevocationFailed = `-- name: MarkRevocationFailed :exec
UPDATE revocation_outbox
SET status = 'failed'
WHERE id = $1
`

// Mark a revocation signal as permanently failed (dead-letter). Called when
// TTL (24h) expires or max retries exceeded. Triggers alerting.
func (q *Queries) MarkRevocationFailed(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, markRevocationFailed, id)
	return err
}
