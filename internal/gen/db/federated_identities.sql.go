// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: federated_identities.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createFederatedIdentity = `-- name: CreateFederatedIdentity :one
INSERT INTO federated_identities (
    namespace_id, subject_hmac, key_version, user_id
) VALUES (
    $1, $2, $3, $4
)
RETURNING namespace_id, subject_hmac, key_version, user_id, created_at, last_seen_at
`

type CreateFederatedIdentityParams struct {
	NamespaceID string      `json:"namespace_id"`
	SubjectHmac []byte      `json:"subject_hmac"`
	KeyVersion  int16       `json:"key_version"`
	UserID      pgtype.UUID `json:"user_id"`
}

// user_id must always be an id THIS request just created via CreateFederatedUser
// (see internal/cloudapi/federated_store.go's ResolveOrCreateFederatedUser) —
// this query never binds a federated subject to a pre-existing user.
func (q *Queries) CreateFederatedIdentity(ctx context.Context, arg CreateFederatedIdentityParams) (FederatedIdentity, error) {
	row := q.db.QueryRow(ctx, createFederatedIdentity,
		arg.NamespaceID,
		arg.SubjectHmac,
		arg.KeyVersion,
		arg.UserID,
	)
	var i FederatedIdentity
	err := row.Scan(
		&i.NamespaceID,
		&i.SubjectHmac,
		&i.KeyVersion,
		&i.UserID,
		&i.CreatedAt,
		&i.LastSeenAt,
	)
	return i, err
}

const deleteFederatedIdentitiesByNamespace = `-- name: DeleteFederatedIdentitiesByNamespace :exec
DELETE FROM federated_identities
WHERE namespace_id = $1
`

// Removes every federated identity mapping for a namespace. Called when a
// namespace is deleted (cloud_namespaces soft-delete, internal/cloudapi) so a
// future namespace id reuse can never resolve through a stale mapping.
func (q *Queries) DeleteFederatedIdentitiesByNamespace(ctx context.Context, namespaceID string) error {
	_, err := q.db.Exec(ctx, deleteFederatedIdentitiesByNamespace, namespaceID)
	return err
}

const getFederatedIdentity = `-- name: GetFederatedIdentity :one

SELECT namespace_id, subject_hmac, key_version, user_id, created_at, last_seen_at FROM federated_identities
WHERE namespace_id = $1 AND subject_hmac = $2 AND key_version = $3
`

type GetFederatedIdentityParams struct {
	NamespaceID string `json:"namespace_id"`
	SubjectHmac []byte `json:"subject_hmac"`
	KeyVersion  int16  `json:"key_version"`
}

// Queries for the federated_identities table (db/migrations/0021) — the
// corporate-SSO handoff's namespace-scoped external-subject -> Harbor user_id
// mapping. The query IS the contract (DESIGN §1.3): `sqlc generate` produces
// typed Go — never hand-write DB types.
func (q *Queries) GetFederatedIdentity(ctx context.Context, arg GetFederatedIdentityParams) (FederatedIdentity, error) {
	row := q.db.QueryRow(ctx, getFederatedIdentity, arg.NamespaceID, arg.SubjectHmac, arg.KeyVersion)
	var i FederatedIdentity
	err := row.Scan(
		&i.NamespaceID,
		&i.SubjectHmac,
		&i.KeyVersion,
		&i.UserID,
		&i.CreatedAt,
		&i.LastSeenAt,
	)
	return i, err
}

const touchFederatedIdentity = `-- name: TouchFederatedIdentity :exec
UPDATE federated_identities
SET last_seen_at = now()
WHERE namespace_id = $1 AND subject_hmac = $2 AND key_version = $3
`

type TouchFederatedIdentityParams struct {
	NamespaceID string `json:"namespace_id"`
	SubjectHmac []byte `json:"subject_hmac"`
	KeyVersion  int16  `json:"key_version"`
}

// Bumps last_seen_at on an existing mapping (called on every successful
// resolve, not just creation, so the row reflects the subject's most recent
// SSO login).
func (q *Queries) TouchFederatedIdentity(ctx context.Context, arg TouchFederatedIdentityParams) error {
	_, err := q.db.Exec(ctx, touchFederatedIdentity, arg.NamespaceID, arg.SubjectHmac, arg.KeyVersion)
	return err
}
