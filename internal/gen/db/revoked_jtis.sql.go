// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: revoked_jtis.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const gCExpiredRevokedJTIs = `-- name: GCExpiredRevokedJTIs :exec
DELETE FROM revoked_jtis
WHERE expires_at < now()
`

// GCExpiredRevokedJTIs deletes entries for JWTs that have expired — their JTIs
// no longer need revocation (the JWT itself is invalid). Run nightly as
// background cleanup, off the hot path.
func (q *Queries) GCExpiredRevokedJTIs(ctx context.Context) error {
	_, err := q.db.Exec(ctx, gCExpiredRevokedJTIs)
	return err
}

const getRevokedJTI = `-- name: GetRevokedJTI :one
SELECT jti, revoked_at, reason, expires_at FROM revoked_jtis
WHERE jti = $1
`

// GetRevokedJTI checks if a specific JTI is revoked. Used for introspection
// fallback when the bloom filter returns a hit (confirms true positive vs
// false positive).
func (q *Queries) GetRevokedJTI(ctx context.Context, jti string) (RevokedJti, error) {
	row := q.db.QueryRow(ctx, getRevokedJTI, jti)
	var i RevokedJti
	err := row.Scan(
		&i.Jti,
		&i.RevokedAt,
		&i.Reason,
		&i.ExpiresAt,
	)
	return i, err
}

const insertRevokedJTI = `-- name: InsertRevokedJTI :one

INSERT INTO revoked_jtis (jti, reason, expires_at)
VALUES ($1, $2, $3)
ON CONFLICT (jti) DO UPDATE
SET reason = EXCLUDED.reason,
    expires_at = EXCLUDED.expires_at,
    revoked_at = now()
RETURNING jti, revoked_at, reason, expires_at
`

type InsertRevokedJTIParams struct {
	Jti       string             `json:"jti"`
	Reason    string             `json:"reason"`
	ExpiresAt pgtype.Timestamptz `json:"expires_at"`
}

// Queries for the revoked_jtis table (emergency JWT revocation via bloom filter;
// DESIGN §3.5). The query IS the contract (DESIGN §1.3): `sqlc generate` (via
// @codegen) produces typed Go — never hand-write DB types.
//
// This table is the persistent source of truth for revoked JTIs. The in-process
// bloom filter is rehydrated from ListActive on startup and kept in sync via
// Redis pub/sub.
// InsertRevokedJTI upserts a revoked JTI. On conflict (re-revocation of same
// JTI), update reason/expires_at to latest values. This is idempotent so
// retries and duplicate pub/sub messages are safe.
func (q *Queries) InsertRevokedJTI(ctx context.Context, arg InsertRevokedJTIParams) (RevokedJti, error) {
	row := q.db.QueryRow(ctx, insertRevokedJTI, arg.Jti, arg.Reason, arg.ExpiresAt)
	var i RevokedJti
	err := row.Scan(
		&i.Jti,
		&i.RevokedAt,
		&i.Reason,
		&i.ExpiresAt,
	)
	return i, err
}

const listActiveRevokedJTIs = `-- name: ListActiveRevokedJTIs :many
SELECT jti, revoked_at, reason, expires_at FROM revoked_jtis
WHERE expires_at > now()
ORDER BY revoked_at DESC
`

// ListActiveRevokedJTIs returns all JTIs that are still within their JWT's
// original expiry window. Used to rehydrate the bloom filter on startup.
func (q *Queries) ListActiveRevokedJTIs(ctx context.Context) ([]RevokedJti, error) {
	rows, err := q.db.Query(ctx, listActiveRevokedJTIs)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []RevokedJti
	for rows.Next() {
		var i RevokedJti
		if err := rows.Scan(
			&i.Jti,
			&i.RevokedAt,
			&i.Reason,
			&i.ExpiresAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
