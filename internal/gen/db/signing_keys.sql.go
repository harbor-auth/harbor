// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: signing_keys.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createSigningKey = `-- name: CreateSigningKey :one

INSERT INTO signing_keys (
    id, kid, state, public_key_bytes, private_key_wrapped, region
) VALUES (
    $1, $2, 'pending', $3, $4, $5
)
RETURNING id, kid, state, public_key_bytes, private_key_wrapped, region, created_at, promoted_at, retired_at
`

type CreateSigningKeyParams struct {
	ID                pgtype.UUID `json:"id"`
	Kid               string      `json:"kid"`
	PublicKeyBytes    []byte      `json:"public_key_bytes"`
	PrivateKeyWrapped []byte      `json:"private_key_wrapped"`
	Region            string      `json:"region"`
}

// Queries for the signing_keys table (JWKS kid rotation; DESIGN §7.3, §3.5.4).
// The query IS the contract (DESIGN §1.3): `sqlc generate` (via @codegen)
// produces typed Go — never hand-write DB types.
//
// Key lifecycle: pending → active → retired. Exactly one key can be active
// at any time (the signer). Pending keys are published in JWKS (giving RPs
// time to cache) but not yet signing. Retired keys are removed from JWKS.
// Creates a new signing key in 'pending' state. The key will be promoted to
// 'active' after the grace period (allowing RP JWKS cache refresh).
func (q *Queries) CreateSigningKey(ctx context.Context, arg CreateSigningKeyParams) (SigningKey, error) {
	row := q.db.QueryRow(ctx, createSigningKey,
		arg.ID,
		arg.Kid,
		arg.PublicKeyBytes,
		arg.PrivateKeyWrapped,
		arg.Region,
	)
	var i SigningKey
	err := row.Scan(
		&i.ID,
		&i.Kid,
		&i.State,
		&i.PublicKeyBytes,
		&i.PrivateKeyWrapped,
		&i.Region,
		&i.CreatedAt,
		&i.PromotedAt,
		&i.RetiredAt,
	)
	return i, err
}

const getActiveSigningKey = `-- name: GetActiveSigningKey :one
SELECT id, kid, state, public_key_bytes, private_key_wrapped, region, created_at, promoted_at, retired_at FROM signing_keys
WHERE state = 'active'
`

// Returns the single active signing key used for signing new tokens.
// The partial unique index idx_signing_keys_one_active guarantees at most one.
func (q *Queries) GetActiveSigningKey(ctx context.Context) (SigningKey, error) {
	row := q.db.QueryRow(ctx, getActiveSigningKey)
	var i SigningKey
	err := row.Scan(
		&i.ID,
		&i.Kid,
		&i.State,
		&i.PublicKeyBytes,
		&i.PrivateKeyWrapped,
		&i.Region,
		&i.CreatedAt,
		&i.PromotedAt,
		&i.RetiredAt,
	)
	return i, err
}

const getSigningKeyByKid = `-- name: GetSigningKeyByKid :one
SELECT id, kid, state, public_key_bytes, private_key_wrapped, region, created_at, promoted_at, retired_at FROM signing_keys
WHERE kid = $1
`

// Retrieves a signing key by its kid (key identifier). Used for token
// verification when the kid is known from the JWT header.
func (q *Queries) GetSigningKeyByKid(ctx context.Context, kid string) (SigningKey, error) {
	row := q.db.QueryRow(ctx, getSigningKeyByKid, kid)
	var i SigningKey
	err := row.Scan(
		&i.ID,
		&i.Kid,
		&i.State,
		&i.PublicKeyBytes,
		&i.PrivateKeyWrapped,
		&i.Region,
		&i.CreatedAt,
		&i.PromotedAt,
		&i.RetiredAt,
	)
	return i, err
}

const listLiveSigningKeys = `-- name: ListLiveSigningKeys :many
SELECT id, kid, state, public_key_bytes, private_key_wrapped, region, created_at, promoted_at, retired_at FROM signing_keys
WHERE state IN ('pending', 'active')
ORDER BY created_at DESC
`

// Returns all keys that should appear in the JWKS endpoint: pending (new keys
// awaiting promotion) and active (the current signer). Retired keys are
// excluded — tokens signed by retired keys will fail verification.
func (q *Queries) ListLiveSigningKeys(ctx context.Context) ([]SigningKey, error) {
	rows, err := q.db.Query(ctx, listLiveSigningKeys)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SigningKey
	for rows.Next() {
		var i SigningKey
		if err := rows.Scan(
			&i.ID,
			&i.Kid,
			&i.State,
			&i.PublicKeyBytes,
			&i.PrivateKeyWrapped,
			&i.Region,
			&i.CreatedAt,
			&i.PromotedAt,
			&i.RetiredAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const retireSigningKey = `-- name: RetireSigningKey :one
UPDATE signing_keys
SET state = 'retired',
    retired_at = now()
WHERE kid = $1
  AND state IN ('pending', 'active')
RETURNING id, kid, state, public_key_bytes, private_key_wrapped, region, created_at, promoted_at, retired_at
`

// Convenience query to retire a key by kid. Sets state to 'retired' and
// retired_at to now(). Used during scheduled rotation (after overlap window)
// or emergency rotation (immediate).
func (q *Queries) RetireSigningKey(ctx context.Context, kid string) (SigningKey, error) {
	row := q.db.QueryRow(ctx, retireSigningKey, kid)
	var i SigningKey
	err := row.Scan(
		&i.ID,
		&i.Kid,
		&i.State,
		&i.PublicKeyBytes,
		&i.PrivateKeyWrapped,
		&i.Region,
		&i.CreatedAt,
		&i.PromotedAt,
		&i.RetiredAt,
	)
	return i, err
}

const updateSigningKeyState = `-- name: UpdateSigningKeyState :one
UPDATE signing_keys
SET state = $2,
    promoted_at = $3,
    retired_at = $4
WHERE id = $1
RETURNING id, kid, state, public_key_bytes, private_key_wrapped, region, created_at, promoted_at, retired_at
`

type UpdateSigningKeyStateParams struct {
	ID         pgtype.UUID        `json:"id"`
	State      string             `json:"state"`
	PromotedAt pgtype.Timestamptz `json:"promoted_at"`
	RetiredAt  pgtype.Timestamptz `json:"retired_at"`
}

// Promotes a key from pending → active or active → retired. The caller must
// set promoted_at when promoting to active, and retired_at when retiring.
// The CHECK constraint signing_keys_state_timestamps enforces invariants.
func (q *Queries) UpdateSigningKeyState(ctx context.Context, arg UpdateSigningKeyStateParams) (SigningKey, error) {
	row := q.db.QueryRow(ctx, updateSigningKeyState,
		arg.ID,
		arg.State,
		arg.PromotedAt,
		arg.RetiredAt,
	)
	var i SigningKey
	err := row.Scan(
		&i.ID,
		&i.Kid,
		&i.State,
		&i.PublicKeyBytes,
		&i.PrivateKeyWrapped,
		&i.Region,
		&i.CreatedAt,
		&i.PromotedAt,
		&i.RetiredAt,
	)
	return i, err
}
