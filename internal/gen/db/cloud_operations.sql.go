// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: cloud_operations.sql

package db

import (
	"context"
)

const createCloudOperation = `-- name: CreateCloudOperation :one

INSERT INTO cloud_operations (
    idempotency_key, operation, request_hash, response_body
) VALUES (
    $1, $2, $3, $4
)
RETURNING idempotency_key, operation, request_hash, response_body, created_at
`

type CreateCloudOperationParams struct {
	IdempotencyKey string `json:"idempotency_key"`
	Operation      string `json:"operation"`
	RequestHash    []byte `json:"request_hash"`
	ResponseBody   []byte `json:"response_body"`
}

// Queries for the cloud_operations table — the idempotency ledger shared by
// namespace create/delete and session minting (Harbor Cloud management API
// contract; openspec/changes/harbor-cloud-management-api-contract-2ee993ea).
// The query IS the contract (DESIGN §1.3): `sqlc generate` (via @codegen)
// produces typed Go — never hand-write DB types.
// Records the first response for a given (idempotency_key, operation) pair.
// A concurrent duplicate insert violates the primary key; the caller maps
// that race to the same "replay the existing row" path as a normal lookup.
func (q *Queries) CreateCloudOperation(ctx context.Context, arg CreateCloudOperationParams) (CloudOperation, error) {
	row := q.db.QueryRow(ctx, createCloudOperation,
		arg.IdempotencyKey,
		arg.Operation,
		arg.RequestHash,
		arg.ResponseBody,
	)
	var i CloudOperation
	err := row.Scan(
		&i.IdempotencyKey,
		&i.Operation,
		&i.RequestHash,
		&i.ResponseBody,
		&i.CreatedAt,
	)
	return i, err
}

const getCloudOperation = `-- name: GetCloudOperation :one
SELECT idempotency_key, operation, request_hash, response_body, created_at FROM cloud_operations
WHERE idempotency_key = $1
  AND operation = $2
`

type GetCloudOperationParams struct {
	IdempotencyKey string `json:"idempotency_key"`
	Operation      string `json:"operation"`
}

// Looks up a prior operation by its idempotency key and operation name. The
// caller compares request_hash to decide between replaying response_body
// (same hash) and 409 idempotency_key_reused (different hash).
func (q *Queries) GetCloudOperation(ctx context.Context, arg GetCloudOperationParams) (CloudOperation, error) {
	row := q.db.QueryRow(ctx, getCloudOperation, arg.IdempotencyKey, arg.Operation)
	var i CloudOperation
	err := row.Scan(
		&i.IdempotencyKey,
		&i.Operation,
		&i.RequestHash,
		&i.ResponseBody,
		&i.CreatedAt,
	)
	return i, err
}
