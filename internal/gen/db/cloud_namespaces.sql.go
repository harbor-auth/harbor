// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: cloud_namespaces.sql

package db

import (
	"context"
)

const createCloudNamespace = `-- name: CreateCloudNamespace :one

INSERT INTO cloud_namespaces (
    id, status
) VALUES (
    $1, $2
)
RETURNING id, status, created_at, updated_at, deleted_at
`

type CreateCloudNamespaceParams struct {
	ID     string `json:"id"`
	Status string `json:"status"`
}

// Queries for the cloud_namespaces table (Harbor Cloud management API
// contract; openspec/changes/harbor-cloud-management-api-contract-2ee993ea).
// The query IS the contract (DESIGN §1.3): `sqlc generate` (via @codegen)
// produces typed Go — never hand-write DB types.
// Creates a new namespace provisioning record. The id is caller-supplied
// (Harbor Cloud mints it); a duplicate id violates the primary key and the
// caller maps that to 409 namespace_already_exists.
func (q *Queries) CreateCloudNamespace(ctx context.Context, arg CreateCloudNamespaceParams) (CloudNamespace, error) {
	row := q.db.QueryRow(ctx, createCloudNamespace, arg.ID, arg.Status)
	var i CloudNamespace
	err := row.Scan(
		&i.ID,
		&i.Status,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.DeletedAt,
	)
	return i, err
}

const getCloudNamespace = `-- name: GetCloudNamespace :one
SELECT id, status, created_at, updated_at, deleted_at FROM cloud_namespaces
WHERE id = $1
`

// Returns the namespace row regardless of deleted_at — the caller decides
// whether a soft-deleted row should be treated as not-found (mirrors the
// sessions store's "let the caller interpret lifecycle state" pattern).
func (q *Queries) GetCloudNamespace(ctx context.Context, id string) (CloudNamespace, error) {
	row := q.db.QueryRow(ctx, getCloudNamespace, id)
	var i CloudNamespace
	err := row.Scan(
		&i.ID,
		&i.Status,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.DeletedAt,
	)
	return i, err
}

const softDeleteCloudNamespace = `-- name: SoftDeleteCloudNamespace :exec
UPDATE cloud_namespaces
SET deleted_at = now(),
    updated_at = now()
WHERE id = $1
  AND deleted_at IS NULL
`

// Marks a namespace deleted. Affects zero rows for an absent or
// already-deleted namespace — the caller's DELETE handler treats that as
// success (idempotent delete: 204, always).
func (q *Queries) SoftDeleteCloudNamespace(ctx context.Context, id string) error {
	_, err := q.db.Exec(ctx, softDeleteCloudNamespace, id)
	return err
}
