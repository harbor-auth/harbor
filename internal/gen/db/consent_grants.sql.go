// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: consent_grants.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getConsentGrantByUserClient = `-- name: GetConsentGrantByUserClient :one
SELECT id, user_id, client_id, scopes, granted_at, updated_at, revoked_at FROM consent_grants
WHERE user_id = $1
  AND client_id = $2
  AND revoked_at IS NULL
`

type GetConsentGrantByUserClientParams struct {
	UserID   pgtype.UUID `json:"user_id"`
	ClientID string      `json:"client_id"`
}

// Retrieves the active consent grant for a (user, client) pair.
// Returns NULL if no active grant exists (revoked grants are excluded).
func (q *Queries) GetConsentGrantByUserClient(ctx context.Context, arg GetConsentGrantByUserClientParams) (ConsentGrant, error) {
	row := q.db.QueryRow(ctx, getConsentGrantByUserClient, arg.UserID, arg.ClientID)
	var i ConsentGrant
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.ClientID,
		&i.Scopes,
		&i.GrantedAt,
		&i.UpdatedAt,
		&i.RevokedAt,
	)
	return i, err
}

const listConsentGrantsByUser = `-- name: ListConsentGrantsByUser :many
SELECT id, user_id, client_id, scopes, granted_at, updated_at, revoked_at FROM consent_grants
WHERE user_id = $1
  AND revoked_at IS NULL
ORDER BY granted_at DESC
`

// Lists all active consent grants for a user, ordered by most recent first.
// Used by harbor-mgmt to show the user their connected apps.
func (q *Queries) ListConsentGrantsByUser(ctx context.Context, userID pgtype.UUID) ([]ConsentGrant, error) {
	rows, err := q.db.Query(ctx, listConsentGrantsByUser, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ConsentGrant
	for rows.Next() {
		var i ConsentGrant
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.ClientID,
			&i.Scopes,
			&i.GrantedAt,
			&i.UpdatedAt,
			&i.RevokedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const revokeConsentGrant = `-- name: RevokeConsentGrant :exec
UPDATE consent_grants
SET revoked_at = now()
WHERE id = $1
  AND revoked_at IS NULL
`

// Revokes a consent grant by setting revoked_at. Only affects active grants.
// The partial unique index allows a new grant to be created after revocation.
func (q *Queries) RevokeConsentGrant(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, revokeConsentGrant, id)
	return err
}

const upsertConsentGrant = `-- name: UpsertConsentGrant :one

INSERT INTO consent_grants (
    user_id, client_id, scopes
) VALUES (
    $1, $2, $3
)
ON CONFLICT (user_id, client_id) WHERE revoked_at IS NULL
DO UPDATE SET
    scopes = EXCLUDED.scopes,
    updated_at = now()
RETURNING id, user_id, client_id, scopes, granted_at, updated_at, revoked_at
`

type UpsertConsentGrantParams struct {
	UserID   pgtype.UUID `json:"user_id"`
	ClientID string      `json:"client_id"`
	Scopes   []string    `json:"scopes"`
}

// Queries for the consent_grants table (DESIGN §11). The query IS the contract
// (DESIGN §1.3): `sqlc generate` (via @codegen) produces typed Go — never
// hand-write DB types. Tracks per-(user, RP, scope) consent; enforced at
// /authorize; grant/revoke exposed via harbor-mgmt.
// Inserts a new consent grant or updates an existing active grant's scopes.
// The partial unique index idx_consent_grants_user_client_active ensures only
// one active grant per (user, client) pair. On conflict, we update scopes and
// updated_at to reflect the new consent.
func (q *Queries) UpsertConsentGrant(ctx context.Context, arg UpsertConsentGrantParams) (ConsentGrant, error) {
	row := q.db.QueryRow(ctx, upsertConsentGrant, arg.UserID, arg.ClientID, arg.Scopes)
	var i ConsentGrant
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.ClientID,
		&i.Scopes,
		&i.GrantedAt,
		&i.UpdatedAt,
		&i.RevokedAt,
	)
	return i, err
}
