// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: mfa_factors.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createMFAFactor = `-- name: CreateMFAFactor :one
INSERT INTO mfa_factors (
    id, region, user_id, type, secret, code_hash
) VALUES (
    $1, $2, $3, $4, $5, $6
)
RETURNING id, region, user_id, type, secret, code_hash, used, created_at
`

type CreateMFAFactorParams struct {
	ID       pgtype.UUID `json:"id"`
	Region   string      `json:"region"`
	UserID   pgtype.UUID `json:"user_id"`
	Type     string      `json:"type"`
	Secret   []byte      `json:"secret"`
	CodeHash []byte      `json:"code_hash"`
}

func (q *Queries) CreateMFAFactor(ctx context.Context, arg CreateMFAFactorParams) (MfaFactor, error) {
	row := q.db.QueryRow(ctx, createMFAFactor,
		arg.ID,
		arg.Region,
		arg.UserID,
		arg.Type,
		arg.Secret,
		arg.CodeHash,
	)
	var i MfaFactor
	err := row.Scan(
		&i.ID,
		&i.Region,
		&i.UserID,
		&i.Type,
		&i.Secret,
		&i.CodeHash,
		&i.Used,
		&i.CreatedAt,
	)
	return i, err
}

const deleteMFAFactor = `-- name: DeleteMFAFactor :exec
DELETE FROM mfa_factors
WHERE id = $1
`

func (q *Queries) DeleteMFAFactor(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, deleteMFAFactor, id)
	return err
}

const getMFAFactor = `-- name: GetMFAFactor :one

SELECT id, region, user_id, type, secret, code_hash, used, created_at FROM mfa_factors
WHERE id = $1
`

// Queries for the mfa_factors table (encrypted TOTP secrets + hashed recovery
// codes; DESIGN §10). The query IS the contract (DESIGN §1.3): `sqlc generate`
// (via @codegen) produces typed Go — never hand-write DB types.
func (q *Queries) GetMFAFactor(ctx context.Context, id pgtype.UUID) (MfaFactor, error) {
	row := q.db.QueryRow(ctx, getMFAFactor, id)
	var i MfaFactor
	err := row.Scan(
		&i.ID,
		&i.Region,
		&i.UserID,
		&i.Type,
		&i.Secret,
		&i.CodeHash,
		&i.Used,
		&i.CreatedAt,
	)
	return i, err
}

const listMFAFactorsByUser = `-- name: ListMFAFactorsByUser :many
SELECT id, region, user_id, type, secret, code_hash, used, created_at FROM mfa_factors
WHERE user_id = $1
ORDER BY created_at DESC
`

func (q *Queries) ListMFAFactorsByUser(ctx context.Context, userID pgtype.UUID) ([]MfaFactor, error) {
	rows, err := q.db.Query(ctx, listMFAFactorsByUser, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []MfaFactor
	for rows.Next() {
		var i MfaFactor
		if err := rows.Scan(
			&i.ID,
			&i.Region,
			&i.UserID,
			&i.Type,
			&i.Secret,
			&i.CodeHash,
			&i.Used,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const markMFAFactorUsed = `-- name: MarkMFAFactorUsed :exec
UPDATE mfa_factors
SET used = true
WHERE id = $1
  AND used = false
`

// MarkMFAFactorUsed burns a one-time factor (e.g. a recovery code) so it can't
// be replayed. Only flips unused → used, so a double-spend is a no-op.
func (q *Queries) MarkMFAFactorUsed(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, markMFAFactorUsed, id)
	return err
}
