// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: recovery_codes.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const consumeRecoveryCode = `-- name: ConsumeRecoveryCode :one
UPDATE recovery_codes
SET used_at = now()
WHERE id = $1
  AND used_at IS NULL
RETURNING id, user_id, code_hash, salt, used_at, created_at
`

// Atomically marks a recovery code as used (only if unused). Returns the
// updated row if successful, or no rows if the code was already used.
// This is the core fail-closed operation: a code can only be consumed once.
func (q *Queries) ConsumeRecoveryCode(ctx context.Context, id pgtype.UUID) (RecoveryCode, error) {
	row := q.db.QueryRow(ctx, consumeRecoveryCode, id)
	var i RecoveryCode
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.CodeHash,
		&i.Salt,
		&i.UsedAt,
		&i.CreatedAt,
	)
	return i, err
}

const countUnusedCodes = `-- name: CountUnusedCodes :one
SELECT COUNT(*) FROM recovery_codes
WHERE user_id = $1
  AND used_at IS NULL
`

// Counts remaining unused recovery codes for a user. Used to warn users
// when they're running low on codes and should generate new ones.
func (q *Queries) CountUnusedCodes(ctx context.Context, userID pgtype.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countUnusedCodes, userID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const deleteRecoveryCodesByUser = `-- name: DeleteRecoveryCodesByUser :exec
DELETE FROM recovery_codes
WHERE user_id = $1
`

// Deletes all recovery codes for a user. Used when regenerating codes
// (old codes are invalidated when new ones are issued).
func (q *Queries) DeleteRecoveryCodesByUser(ctx context.Context, userID pgtype.UUID) error {
	_, err := q.db.Exec(ctx, deleteRecoveryCodesByUser, userID)
	return err
}

const getRecoveryAttempts = `-- name: GetRecoveryAttempts :one
SELECT user_id, failed_count, locked_until FROM recovery_attempts
WHERE user_id = $1
`

// Gets the current lockout state for a user. Returns NULL if no record
// exists (user has never failed a recovery attempt).
func (q *Queries) GetRecoveryAttempts(ctx context.Context, userID pgtype.UUID) (RecoveryAttempt, error) {
	row := q.db.QueryRow(ctx, getRecoveryAttempts, userID)
	var i RecoveryAttempt
	err := row.Scan(&i.UserID, &i.FailedCount, &i.LockedUntil)
	return i, err
}

const getRecoveryCodesByUser = `-- name: GetRecoveryCodesByUser :many
SELECT id, user_id, code_hash, salt, used_at, created_at FROM recovery_codes
WHERE user_id = $1
ORDER BY created_at
`

// Retrieves all recovery codes for a user (used and unused).
// Caller uses this to validate a submitted code against the hashes.
func (q *Queries) GetRecoveryCodesByUser(ctx context.Context, userID pgtype.UUID) ([]RecoveryCode, error) {
	rows, err := q.db.Query(ctx, getRecoveryCodesByUser, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []RecoveryCode
	for rows.Next() {
		var i RecoveryCode
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.CodeHash,
			&i.Salt,
			&i.UsedAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

type InsertRecoveryCodesParams struct {
	ID        pgtype.UUID        `json:"id"`
	UserID    pgtype.UUID        `json:"user_id"`
	CodeHash  []byte             `json:"code_hash"`
	Salt      []byte             `json:"salt"`
	CreatedAt pgtype.Timestamptz `json:"created_at"`
}

const resetRecoveryAttempts = `-- name: ResetRecoveryAttempts :exec
UPDATE recovery_attempts
SET failed_count = 0,
    locked_until = NULL
WHERE user_id = $1
`

// Resets the lockout state for a user after successful recovery.
// Clears failed_count and locked_until.
func (q *Queries) ResetRecoveryAttempts(ctx context.Context, userID pgtype.UUID) error {
	_, err := q.db.Exec(ctx, resetRecoveryAttempts, userID)
	return err
}

const upsertRecoveryAttempts = `-- name: UpsertRecoveryAttempts :one
INSERT INTO recovery_attempts (
    user_id, failed_count, locked_until
) VALUES (
    $1, $2, $3
)
ON CONFLICT (user_id)
DO UPDATE SET
    failed_count = EXCLUDED.failed_count,
    locked_until = EXCLUDED.locked_until
RETURNING user_id, failed_count, locked_until
`

type UpsertRecoveryAttemptsParams struct {
	UserID      pgtype.UUID        `json:"user_id"`
	FailedCount int32              `json:"failed_count"`
	LockedUntil pgtype.Timestamptz `json:"locked_until"`
}

// Upserts the recovery attempts record for a user. Used to increment
// failed_count or set locked_until after too many failures.
func (q *Queries) UpsertRecoveryAttempts(ctx context.Context, arg UpsertRecoveryAttemptsParams) (RecoveryAttempt, error) {
	row := q.db.QueryRow(ctx, upsertRecoveryAttempts, arg.UserID, arg.FailedCount, arg.LockedUntil)
	var i RecoveryAttempt
	err := row.Scan(&i.UserID, &i.FailedCount, &i.LockedUntil)
	return i, err
}
