// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: relay_addresses.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createRelayAddress = `-- name: CreateRelayAddress :one

INSERT INTO relay_addresses (
    relay_token, user_id, client_id, state, enc_mapping, region
) VALUES (
    $1, $2, $3, $4, $5, $6
)
RETURNING id, relay_token, user_id, client_id, state, enc_mapping, region, created_at, deactivated_at
`

type CreateRelayAddressParams struct {
	RelayToken string      `json:"relay_token"`
	UserID     pgtype.UUID `json:"user_id"`
	ClientID   string      `json:"client_id"`
	State      string      `json:"state"`
	EncMapping []byte      `json:"enc_mapping"`
	Region     string      `json:"region"`
}

// Queries for the relay_addresses table (DESIGN §7.5). The query IS the contract
// (DESIGN §1.3): `sqlc generate` (via @codegen) produces typed Go — never
// hand-write DB types. Stores per-(user, RP) email relay mappings with
// envelope-encrypted real email, region-pinned and never cross-region replicated.
// Mints a new relay address for a (user, client) pair. The unique index
// idx_relay_addresses_user_client enforces one relay address per (user, RP).
// The relay_token is an opaque, unlinkable token (randomly generated, not
// derived from user_id). The enc_mapping holds the envelope-encrypted real
// email address.
func (q *Queries) CreateRelayAddress(ctx context.Context, arg CreateRelayAddressParams) (RelayAddress, error) {
	row := q.db.QueryRow(ctx, createRelayAddress,
		arg.RelayToken,
		arg.UserID,
		arg.ClientID,
		arg.State,
		arg.EncMapping,
		arg.Region,
	)
	var i RelayAddress
	err := row.Scan(
		&i.ID,
		&i.RelayToken,
		&i.UserID,
		&i.ClientID,
		&i.State,
		&i.EncMapping,
		&i.Region,
		&i.CreatedAt,
		&i.DeactivatedAt,
	)
	return i, err
}

const deactivateRelayAddress = `-- name: DeactivateRelayAddress :exec
UPDATE relay_addresses
SET state = 'deactivated',
    deactivated_at = now()
WHERE id = $1
  AND state = 'active'
`

// Deactivates a relay address (hard-bounce kill switch). Sets state to
// 'deactivated' and records deactivated_at timestamp. Deactivation is
// independent of login grant revocation (§7.5.4).
func (q *Queries) DeactivateRelayAddress(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, deactivateRelayAddress, id)
	return err
}

const deactivateRelayAddressByUserClient = `-- name: DeactivateRelayAddressByUserClient :exec
UPDATE relay_addresses
SET state = 'deactivated',
    deactivated_at = now()
WHERE user_id = $1
  AND client_id = $2
  AND state = 'active'
`

type DeactivateRelayAddressByUserClientParams struct {
	UserID   pgtype.UUID `json:"user_id"`
	ClientID string      `json:"client_id"`
}

// Deactivates a relay address by (user_id, client_id) pair. Convenience
// method for when the caller has the user/client but not the address ID.
func (q *Queries) DeactivateRelayAddressByUserClient(ctx context.Context, arg DeactivateRelayAddressByUserClientParams) error {
	_, err := q.db.Exec(ctx, deactivateRelayAddressByUserClient, arg.UserID, arg.ClientID)
	return err
}

const getActiveRelayAddressByToken = `-- name: GetActiveRelayAddressByToken :one
SELECT id, relay_token, user_id, client_id, state, enc_mapping, region, created_at, deactivated_at FROM relay_addresses
WHERE relay_token = $1
  AND state = 'active'
`

// Region-pinned lookup by relay_token for active addresses only. Used by the
// inbound MTA to reject mail to deactivated addresses (hard-bounce kill switch).
func (q *Queries) GetActiveRelayAddressByToken(ctx context.Context, relayToken string) (RelayAddress, error) {
	row := q.db.QueryRow(ctx, getActiveRelayAddressByToken, relayToken)
	var i RelayAddress
	err := row.Scan(
		&i.ID,
		&i.RelayToken,
		&i.UserID,
		&i.ClientID,
		&i.State,
		&i.EncMapping,
		&i.Region,
		&i.CreatedAt,
		&i.DeactivatedAt,
	)
	return i, err
}

const getRelayAddressByToken = `-- name: GetRelayAddressByToken :one
SELECT id, relay_token, user_id, client_id, state, enc_mapping, region, created_at, deactivated_at FROM relay_addresses
WHERE relay_token = $1
`

// Region-pinned lookup by relay_token — the inbound MTA path. The caller MUST
// verify the region matches the user's home region (regional data residency).
// Returns the full row including enc_mapping for decryption.
func (q *Queries) GetRelayAddressByToken(ctx context.Context, relayToken string) (RelayAddress, error) {
	row := q.db.QueryRow(ctx, getRelayAddressByToken, relayToken)
	var i RelayAddress
	err := row.Scan(
		&i.ID,
		&i.RelayToken,
		&i.UserID,
		&i.ClientID,
		&i.State,
		&i.EncMapping,
		&i.Region,
		&i.CreatedAt,
		&i.DeactivatedAt,
	)
	return i, err
}

const getRelayAddressByUserClient = `-- name: GetRelayAddressByUserClient :one
SELECT id, relay_token, user_id, client_id, state, enc_mapping, region, created_at, deactivated_at FROM relay_addresses
WHERE user_id = $1
  AND client_id = $2
`

type GetRelayAddressByUserClientParams struct {
	UserID   pgtype.UUID `json:"user_id"`
	ClientID string      `json:"client_id"`
}

// Lookup by (user_id, client_id) pair — used to check if a relay address
// already exists before minting a new one.
func (q *Queries) GetRelayAddressByUserClient(ctx context.Context, arg GetRelayAddressByUserClientParams) (RelayAddress, error) {
	row := q.db.QueryRow(ctx, getRelayAddressByUserClient, arg.UserID, arg.ClientID)
	var i RelayAddress
	err := row.Scan(
		&i.ID,
		&i.RelayToken,
		&i.UserID,
		&i.ClientID,
		&i.State,
		&i.EncMapping,
		&i.Region,
		&i.CreatedAt,
		&i.DeactivatedAt,
	)
	return i, err
}

const listRelayAddressesByUser = `-- name: ListRelayAddressesByUser :many
SELECT id, relay_token, user_id, client_id, state, enc_mapping, region, created_at, deactivated_at FROM relay_addresses
WHERE user_id = $1
ORDER BY created_at DESC
`

// Lists all relay addresses for a user, ordered by most recent first.
// Used by harbor-mgmt to show the user their relay addresses per RP.
func (q *Queries) ListRelayAddressesByUser(ctx context.Context, userID pgtype.UUID) ([]RelayAddress, error) {
	rows, err := q.db.Query(ctx, listRelayAddressesByUser, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []RelayAddress
	for rows.Next() {
		var i RelayAddress
		if err := rows.Scan(
			&i.ID,
			&i.RelayToken,
			&i.UserID,
			&i.ClientID,
			&i.State,
			&i.EncMapping,
			&i.Region,
			&i.CreatedAt,
			&i.DeactivatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const reactivateRelayAddress = `-- name: ReactivateRelayAddress :exec
UPDATE relay_addresses
SET state = 'active',
    deactivated_at = NULL
WHERE id = $1
  AND state = 'deactivated'
`

// Reactivates a previously deactivated relay address. Clears deactivated_at
// and sets state back to 'active'.
func (q *Queries) ReactivateRelayAddress(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, reactivateRelayAddress, id)
	return err
}

const setRelayAddressBYODomain = `-- name: SetRelayAddressBYODomain :exec
UPDATE relay_addresses
SET state = 'byo_domain'
WHERE id = $1
  AND state = 'active'
`

// Transitions a relay address to BYO-domain state after the user has verified
// ownership of their custom domain.
func (q *Queries) SetRelayAddressBYODomain(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, setRelayAddressBYODomain, id)
	return err
}
