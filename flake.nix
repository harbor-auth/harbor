{
  # Harbor — Foundation F3: Fail-Closed Hermetic Toolchain (the pinning half).
  #
  # This flake provides a dev shell with EXACTLY the toolchain every Harbor
  # check needs, pinned to one nixpkgs revision so every agent, every human, and
  # CI run the identical tool versions. Combined with the fail-closed Makefile
  # (the other half of F3), this makes "green means green" true everywhere:
  #
  #   local:  nix develop -c make agent-check
  #   CI:     nix develop -c make agent-check   (.github/workflows/ci.yml)
  #
  # Because the Makefile FAILS CLOSED on a missing tool (a skipped check that
  # passes is a lie to the agent), the whole point of this shell is to guarantee
  # the tools are present and identical — so the checks actually run.
  #
  # `flake.lock` is generated by `nix flake update` (do NOT hand-write it); it
  # freezes the nixpkgs revision that pins the versions below.
  description = "Harbor — pinned, hermetic developer toolchain (Foundation F3)";

  inputs = {
    # Pinned to a stable channel; `nix flake update` refreshes flake.lock.
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-25.05";
    flake-utils.url = "github:numtide/flake-utils";
  };

  outputs = { self, nixpkgs, flake-utils }:
    flake-utils.lib.eachDefaultSystem (system:
      let
        pkgs = import nixpkgs { inherit system; };

        # Go 1.26 is required by go.mod. Prefer the pinned attribute when the
        # channel provides it; otherwise fall back to the channel default `go`
        # (still pinned by flake.lock) — bump the channel if it lags go.mod.
        goToolchain = pkgs.go_1_26 or pkgs.go;

        # The pinned toolchain — one list, mirrored by the Makefile's install
        # hints. Every tool the fail-closed Makefile requires lives here.
        toolchain = [
          goToolchain
          pkgs.golangci-lint            # @validate: lint
          pkgs.sqlc                     # @codegen: sqlc generate
          pkgs.oapi-codegen             # @codegen: OpenAPI -> Go stubs
          pkgs.buf                      # @codegen/@validate: proto gen + lint
          pkgs.golang-migrate           # @db-migrate: schema migrations (`migrate`)
          pkgs.k6                       # @load-test: hot-path load tests
          pkgs.nodejs                   # web codegen runtime (also Node>=20.19 for openspec)
          pkgs.pnpm                     # @codegen: pnpm codegen (web client)
          pkgs.spectral-cli             # @validate: spectral lint (OpenAPI) — provides `spectral`
        ];

        # OPTIONAL, resilient tools — on PATH when the channel provides them,
        # but a missing attribute must NEVER hard-break `nix develop`. openspec
        # is skill-only (NOT a gating agent-check tool yet) and @fission-ai/
        # openspec may not be packaged in this channel, so pin it via the
        # `or null` fallback and filter it out when absent (see comment below).
        optionalTools = builtins.filter (p: p != null) [
          (pkgs.openspec or null)       # @openspec: spec-driven dev — provides `openspec` (validate --strict)
        ];
      in
      {
        devShells.default = pkgs.mkShell {
          buildInputs = toolchain ++ optionalTools;

          # spectral (@stoplight/spectral-cli) is pinned via `pkgs.spectral-cli`
          # above (it provides the `spectral` binary), so it is on PATH inside
          # `nix develop` and the agent-check `spectral` step actually runs. If a
          # future nixpkgs channel drops or renames `spectral-cli`, fix it there
          # (the single place) — do not fall back to an on-demand npm install.
          #
          # openspec (@fission-ai/openspec) is pinned OPTIONALLY via
          # `(pkgs.openspec or null)` in `optionalTools` above — it may not exist
          # in this channel yet, and because openspec is skill-only (NOT a gating
          # agent-check tool), a missing attribute must NOT hard-break
          # `nix develop` (unlike the required tools in `toolchain`). It needs
          # Node>=20.19 (satisfied by `pkgs.nodejs` above) and provides the
          # `openspec` binary (@openspec: spec-driven dev, `validate --strict`).
          # When the attribute is absent, the documented install fallback is the
          # pinned Node's npm: `npm install -g @fission-ai/openspec@latest`
          # (see .agents/openspec.md).
          shellHook = ''
            echo 'Harbor hermetic dev shell — pinned toolchain (flake.nix). Run: make agent-check'
          '';
        };
      });
}
