# Harbor — Spectral OpenAPI linting config (@validate / spec-lint CI step).
#
# Auto-discovered by `spectral lint` when run from the repo root. Uses the OAS
# ruleset bundled with spectral-cli (`spectral:oas`), so no network access or
# extra npm packages are needed.
extends:
  - spectral:oas
