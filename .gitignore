# Build output
/bin/
/agentcheck
/testweakening
/harbor-hot
/harbor-mgmt
/harbor-relay
cmd/harbor-hot/harbor-hot
cmd/harbor-mgmt/harbor-mgmt
check-results.json

# Test / coverage
*.out
*.test
coverage.html

# Go
vendor/

# OIDF conformance harness (cloned upstream suite + run artifacts / evidence)
conformance/.suite/
conformance/out/

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Agent session logs (hippo memory / prompt traces — internal, not for commits)
debug/

# Internal tooling state (not for commits)
.weft/
.hippo/

# Local deployment notes (machine-specific, never commit)
deploy/LOCAL.md
