# Tasks

- [ ] Make Postgres/Redis connection and service collaborators required and explicitly size pgx pools.
- [ ] Wire the complete DB/Redis hot-path graph and remove demo/noop/dev boot paths.
- [ ] Wire management enrollment, WebAuthn sessions, dynamic registration, dashboard, and DB domain persistence.
- [ ] Move test doubles out of production reach and add architecture regression rules.
- [ ] Align Helm and Kubernetes environment contracts, Redis wiring, and shared user-DEK KEK values.
- [ ] Update local Docker/README configuration to use real Postgres and Redis.
- [ ] Add container-backed live-graph, cross-replica, refresh-token, registration, enrollment, and startup-failure tests.
- [ ] Run repository and deployment rendering verification.
