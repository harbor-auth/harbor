schema: spec-driven
created: 2026-07-20
goal: Implement Redis-backed per-client + per-IP hot-path rate limiting middleware (429 + Retry-After, fail-open on Redis outage) protecting /introspect, /token, and /authorize
