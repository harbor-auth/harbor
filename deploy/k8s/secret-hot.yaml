# Secret for harbor-hot (the internet-facing OIDC hot path).
#
# SCAFFOLD: the values below are base64-encoded PLACEHOLDERS and MUST be
# replaced before any real deployment. In production these should be injected
# by a secrets manager (Vault / cloud KMS / External Secrets Operator), NOT
# committed to git. This file exists as a shape reference only.
#
# Kept in a SEPARATE Secret from harbor-mgmt so RBAC can scope each binary's
# service account to only the credentials it needs (least privilege).
#
#   DATABASE_URL      — regional Postgres DSN. harbor-hot reads clients/grants/
#                        sessions/secrets from here (docs/DESIGN.md §10).
#   REDIS_URL         — auth-code TTL store. REQUIRED for multi-replica: without
#                        it auth codes live in-process and cross-replica /token
#                        fails (see cmd/harbor-hot/main.go).
#   ADMIN_API_TOKEN   — Bearer token guarding /admin/keys/rotate and
#                        /admin/revoke-jwt. FATAL if unset when DATABASE_URL is
#                        set. Generate: openssl rand -hex 32
#   HARBOR_KMS_SECRET — shared user-DEK KEK. It MUST be identical to the value
#                        projected into harbor-mgmt so hot can unwrap the user
#                        secrets management created. This is distinct from the
#                        regional signing keys selected by KMS_KEY_MAP.
#   MGMT_HOT_PROXY_TOKEN — only read when harbor-mgmt-config.yaml's
#                        CLOUD_INTEGRATION_ENABLED is "true"; see
#                        internal/cloudapi's key-rotation proxy handler.
#                        Placeholder below is inert while integration stays
#                        disabled, but MUST equal harbor-mgmt-secrets'
#                        MGMT_HOT_PROXY_TOKEN exactly once set.
apiVersion: v1
kind: Secret
metadata:
  name: harbor-hot-secrets
  namespace: harbor
  labels:
    app.kubernetes.io/name: harbor-hot
    app.kubernetes.io/part-of: harbor
type: Opaque
stringData:
  # REPLACE all values below with real values from your secrets manager.
  DATABASE_URL: "postgres://harbor_hot:REPLACE_ME@postgres.harbor.svc:5432/harbor?sslmode=require"
  REDIS_URL: "redis://:REPLACE_ME@redis.harbor.svc:6379/0"
  HARBOR_KMS_SECRET: "REPLACE_ME_WITH_SHARED_32_BYTE_USER_DEK_KEK"
  # Bearer token guarding /admin/ endpoints. FATAL if unset when DATABASE_URL
  # is set. Generate: openssl rand -hex 32
  ADMIN_API_TOKEN: "REPLACE_ME_WITH_A_256_BIT_HEX_ADMIN_TOKEN"
  # Unused while CLOUD_INTEGRATION_ENABLED is "false" (harbor-mgmt-config.yaml).
  # Must equal harbor-mgmt-secrets' MGMT_HOT_PROXY_TOKEN exactly.
  MGMT_HOT_PROXY_TOKEN: "REPLACE_ME_WITH_A_256_BIT_CLOUD_PROXY_TOKEN"
